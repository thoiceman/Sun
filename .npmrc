# 默认使用官方 npm 注册表（安装与发布）
registry=https://registry.npmjs.org/

# 为 @sun 作用域显式指定注册表（可避免使用其他镜像时误发）
@sun-x:registry=https://registry.npmjs.org/

# CI 环境注入令牌以无交互发布（需在 CI 设置 NPM_TOKEN）
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# 如需强制所有请求携带认证（常见于私有仓库），可开启：
# always-auth=true

# 如果你开启了 npm 两步验证（2FA），在发布命令前通过环境变量传入：
# NPM_CONFIG_OTP=123456